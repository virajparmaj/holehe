# transport -- generated from the legacy module tree on 2026-08-18.
# `enabled: false` records what the site actually did when measured;
# `plugin: legacy` marks entries not yet ported to declarative steps.

- id: blablacar
  domain: blablacar.com
  category: transport
  method: register
  plugin: legacy
  frequent_rate_limit: true
  enabled: false
  disabled: { status: blocked, measured: 2026-08-18, http_code: 403 }
  canary: { tier: c, discriminating: "unverified" }

# cms -- generated from the legacy module tree on 2026-08-18.
# `enabled: false` records what the site actually did when measured;
# `plugin: legacy` marks entries not yet ported to declarative steps.

- id: atlassian
  domain: atlassian.com
  category: cms
  method: register
  plugin: legacy
  enabled: false
  disabled: { status: parse_failed, measured: 2026-08-18, http_code: 200 }
  canary: { tier: c, discriminating: "unverified" }

- id: gravatar
  domain: en.gravatar.com
  category: cms
  method: other
  plugin: legacy
  canary: { tier: c, discriminating: "unverified" }

- id: voxmedia
  domain: voxmedia.com
  category: cms
  method: register
  plugin: legacy
  enabled: false
  disabled: { status: server_error, measured: 2026-08-18, http_code: 500 }
  canary: { tier: c, discriminating: "unverified" }

- id: wordpress
  domain: wordpress.com
  category: cms
  method: login
  plugin: legacy
  side_effect: failed_login
  canary: { tier: c, discriminating: "unverified" }

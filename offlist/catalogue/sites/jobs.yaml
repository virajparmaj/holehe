# jobs -- generated from the legacy module tree on 2026-08-18.
# `enabled: false` records what the site actually did when measured;
# `plugin: legacy` marks entries not yet ported to declarative steps.

- id: coroflot
  domain: coroflot.com
  category: jobs
  method: register
  plugin: legacy
  canary: { tier: c, discriminating: "unverified" }

- id: freelancer
  domain: freelancer.com
  category: jobs
  method: register
  plugin: legacy
  canary: { tier: c, discriminating: "yes" }

- id: seoclerks
  domain: seoclerks.com
  category: jobs
  method: register
  plugin: legacy
  canary: { tier: c, discriminating: "yes" }
